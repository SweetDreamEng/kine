import React from 'react'

import './AddButton.scss'

const AddButton = ({ title, fields, buttonText }) => {
  return <div className="addButton">
    <p className="addButtonText">+</p>
    </div>
}

export default AddButton
